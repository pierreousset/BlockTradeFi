import { css } from "@emotion/react";
import Card from "./Card";

export default {
  title: "Components/Card",
  component: Card,
};

export const CardPrimary = () => (
  <Card onClick={() => {}} color="beige">
    Test
  </Card>
);
